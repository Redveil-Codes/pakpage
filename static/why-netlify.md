# Why Netlify?

**Easy for open source**

Netlify was the easiest way to host this site and let in as many contributors as we want, without
handing out the `.env` or any other secrets. On top of that we get a pile of free extras; DNS,
TLS, deploy previews, and more at no cost.
